---
interface Props {
  id?: string;
  eyebrow: string;
  title: string;
  tone?: 'light' | 'dark';
  headingLevel?: 1 | 2;
}

const { id, eyebrow, title, tone = 'light', headingLevel = 2 } = Astro.props;
const isDark = tone === 'dark';
const Heading = headingLevel === 1 ? 'h1' : 'h2';
---
<section
  id={id}
  class:list={[
    'py-16 md:py-20',
    isDark ? 'bg-ink text-white' : 'bg-paper text-ink',
  ]}
>
  <div class="mx-auto max-w-6xl px-4">
    <p class="border-l-4 border-accent pl-3 text-xs font-bold uppercase tracking-widest text-accent">
      {eyebrow}
    </p>
    <Heading class="mt-3 text-3xl font-extrabold tracking-tight md:text-4xl">{title}</Heading>
    <div class="mt-10">
      <slot />
    </div>
  </div>
</section>
